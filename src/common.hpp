#ifndef _COMMON_HPP
#define _COMMON_HPP

#include <vector>
#include <string>
#include <utility>

using Shape = std::vector<size_t>;
using StringPair = std::pair<std::string, std::string>;

#endif // _COMMON_HPP
